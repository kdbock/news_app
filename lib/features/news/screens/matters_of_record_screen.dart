import 'package:flutter/material.dart';

class MattersOfRecordScreen extends StatelessWidget {
  const MattersOfRecordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Matters of Record')),
      body: const Center(child: Text('Matters of Record Screen')),
    );
  }
}
