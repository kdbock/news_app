import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFFd2982a); // Gold
  static const Color secondary = Color(0xFF2d2c31); // Dark Gray

  // Background colors
  static const Color background = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color primaryText = Color(0xFF2d2c31);
  static const Color secondaryText = Colors.grey;
  static const Color inverseText = Colors.white;

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA000);
  static const Color info = Color(0xFF2196F3);

  // Feature-specific colors
  static const Color weatherIcon = Color(0xFFd2982a);
  static const Color sponsoredTag = Color(0xFFd2982a);

  // Shadows and overlays
  static const Color shadow = Color(0x40000000);
  static const Color overlay = Color(0x80000000);
}
